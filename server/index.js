import * as modules from './modules/connector';
import projectOptions from './config';
import { dependencies } from './utils/dependencies';
export default {
  modules,
  projectOptions,
  dependencies,
};
